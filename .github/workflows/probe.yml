name: Probe
on: workflow_dispatch
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "ANDROID_HOME=$ANDROID_HOME"
          which aapt aapt2 apkanalyzer 2>/dev/null || echo "none on PATH"
          find $ANDROID_HOME -name 'aapt2' -type f 2>/dev/null | head -3
