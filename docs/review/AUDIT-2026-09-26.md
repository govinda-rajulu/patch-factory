# Audit: 26 September 2026

Post-PR81 review of source, workflows, Actions history, issues and the public page.
Scope: local source at tree `825f49f9` (commit `d43ad3c`), the overnight watcher
runs, and a bounded public read of upstream projects. No live writes, dispatches,
builds, selection changes or deletions were performed for this audit.

## Fixed in this batch

| ID | Severity | Finding | Fix |
| --- | --- | --- | --- |
| PF-BUILD-001 | High | `src/build/github_bundle.py` validated provider bundle ZIPs with only a CRC check; traversal, symlink, duplicate, encrypted and expansion members were accepted and persisted. | Shared bounded archive inspector (`zip_inspect`/`verify_zip`) now used by both the GitHub and extra-bundle transports; rejects unsafe paths, symlinks, duplicates, encrypted entries and expansion/entry bombs. Regression tests added. |
| PF-WIRE-002 | Medium | Add Target emitted GitLab primary candidates that the resolver and provider watch cannot consume (primaries resolve through GitHub owner/repo only). | GitLab is rejected as a primary candidate at the workflow guard, in preflight, and in validation. Existing GitLab extra-bundle support is unchanged. |
| PF-EXPLORE-003 | Medium | Explore downloaded the first latest-release JAR, checked only size, and executed it, in a credential-persisting checkout. | Explore now installs the patcher only through the verified transport (exact asset, digest, JAR structure) and the checkout does not persist credentials. |
| PF-CI-004 | Medium | Thirteen Python contract suites and two Node contract suites existed in `tests/` but no workflow ran them. | Validation now runs `*_contracts.py` discovery plus the page and notifier contract suites. Two suites gained the same self-contained path bootstrap their siblings already used. |
| PORTAL-001/002 | High (page) | Watch cards joined an independently latest run with an independently latest issue, and saved multipart reports could be silently shortened. | Watch cards now verify run/attempt identity (provider) or the acknowledged receipt key/run (community) before showing a joined status; saved reports render up to producer capacity with an explicit completeness and truncation notice. |
| PORTAL-003 | Medium (page) | Saved-comment reader did not verify multipart completeness or bot authorship. | The reader checks the expected part count and requires every numbered part to be present, bot-authored and unedited; anything less is labeled incomplete with no status inferred. |
| PORTAL-004 | Low (docs) | The guide promised inline original release notes; the page renders only a summary. | Guide wording corrected to a concise summary with full notes on the linked release page. |

## Verified from the overnight runs (observation only)

- Provider watch run `36173375425` attempt 1: 19 expected / 19 attempted / 19 succeeded.
  Reported name deltas are observations for human review against `src/patches/BANNED` and
  `src/patches/CONFIRM`, not selections. The YouTube addition "Remember live stream
  playback position" matches a previously owner-excluded name and stays excluded.
- Community watch run `36173399118` attempt 1: report delivered to issue #82 in 3 parts,
  receipt `READBACK_VERIFIED`, snapshot acknowledged in commit `d43ad3c`.

## Boundaries

- Watchers remain observation-only: no provider wiring, patch selection, build, signing
  or publication authority is inferred from any run or report.
- The Reddit build failures (issues #65, #69, #72) are not root-caused here; exact
  attempt-1 step logs are owner-visible only. PR53 (Dependabot) merge status unknown.
- Third-party upstream projects were inventoried, not fully code-audited.
- No branch-protection change was made; an active ruleset already guards the default
  branch against deletion and non-fast-forward pushes.

## Coverage recheck (owner question, 26 Sep ~07:40 IST)

- **Closed issues read individually:** all 11 closed issues (#22-#25, #28-#34). Five are
  genuinely resolved with current-main evidence (#29, #31-#34); six are automated patch reports
  closed as resolved-but-unverified (#22-#25, #28, #30). Issue #26 was reported closed in an
  earlier inventory but is **open**: it is the GitLab-primary explore report whose unsupported
  path PF-WIRE-002 now rejects. No comment-body review was done.
- **Icons (REQ-027 / R08):** open by design. No official artwork ships; the gate and per-brand
  position are now recorded durably in `docs/review/ICON-PROVENANCE.md`.
- **Agent setup (Jules et al.):** already in the repo, not missing: `AGENTS.md` (the contract
  Jules/Codex/Copilot/Gemini read), `docs/AGENT.md` (architecture), `docs/AGENT-SETUP.md`
  (click-by-click), a CI gate that fails if AGENTS.md stops protecting the listed paths, and
  6. Provider watch running weekly as the no-model baseline. Jules onboarding itself is an
  owner action (personal Google sign-in at jules.google.com), not a repo change. A stale
  "fourteen providers" line in AGENT-SETUP.md was corrected to the actual 19 entries.
